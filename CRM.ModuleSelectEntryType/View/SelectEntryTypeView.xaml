<UserControl x:Class="CRM.ModuleSelectEntryType.View.SelectEntryTypeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
		
		<GroupBox Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
			<StackPanel>
				<RadioButton >Employers</RadioButton>
				<RadioButton>Countries</RadioButton>
			</StackPanel>
		</GroupBox>

		<StackPanel Grid.Row="1" 
					Orientation="Horizontal"
					HorizontalAlignment="Right" VerticalAlignment="Bottom">
			<Button
				Width="36"
				Command="{Binding SelectActionCommand}">Select</Button>
			<Button
				Width="36"
				Command="{Binding ExitCommand}">Exit</Button>
		</StackPanel>
		

	</Grid>
</UserControl>
